<tr>
  <td><%= task.name %></td>
  <td><%= task.done %></td>
  <td><%= link_to "Edit", edit_list_task_path(@list, task) %></td>
  <td><%= link_to "Remove", [@list, task], method: :delete, data: { confirm: "Are you sure?" } %></td>
</tr>
