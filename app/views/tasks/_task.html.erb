<tr id="<%= dom_id(task) %>" >
  <td><%= task.name %></td>
  <td><%= task.done %></td>
  <td><%= link_to "Edit", edit_list_task_path(@list, task) %></td>
  <td><%= link_to "Remove", [@list, task], method: :delete, remote: true %></td>
</tr>
